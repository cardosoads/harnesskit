Read `.harnesskit/state.json` and `.harnesskit/harnesskit.yaml`, then show where this
project is in the Harnesskit process. Prefer running `npx @cardosoads/harnesskit@latest status`
from the project shell. Respond in the configured user language.

Include:
- current user-facing track name;
- current phase and progress;
- active Harness contract count;
- next pending step, if any.
