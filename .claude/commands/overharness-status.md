Read `.overharness/state.json` and `.overharness/overharness.yaml`, then show where this
project is in the OverHarness process. Prefer running `npx overharness@latest status`
from the project shell. Respond in the configured user language.

Include:
- current user-facing track name;
- current phase and progress;
- active Harness contract count;
- next pending step, if any.
