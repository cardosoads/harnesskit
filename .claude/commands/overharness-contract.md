Guide the user through Leslie's Harness contract workflow for the requested work
unit.

Create or revise a contract in `.overharness/harness/contracts/active/` before any
non-trivial implementation. Make must-haves observable, list sensors, set risk,
and decide whether Amy review is required. If the work unit is already clear,
run `npx overharness@latest contract "describe the work unit"`.
